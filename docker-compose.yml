version: '3'

services:

  backend:
    image: ohdaniel/inspire-monolithic
    container_name: monolithic-backend-container
    ports:
      - "8888:8888"
    environment: 
      #DB://database:3306/inspire 
      SPRING_DATASOURSE_USERNAME: root
      SPRING_DATASOURSE_PASWWORD: 12345678
      SPRING_DATASOURCE_URL: jdbc:mariadb://lgcns-daniel.c56oiy82c5g6.us-east-2.rds.amazonaws.com:3306/inspire?useSSL=true&verifyServerCertificate=false

      #JPA
      SPRING_JPA_HIBERNARE_DDL_AUTO: create
      SPRING_JPA_SHOW_SQL: true

      #redis
      SPRING_REDIS_HOST: redis
      SPRING_REDIS_PORT: 6379

    restart: always
    # links:
    #   - database
    depends_on:
      # - database
      - redis
  

      
  # database:
  #   image: ohdaniel/inspire-mariadb
  #   container_name: monolithic-mariadb-container
  #   ports: 
  #     - "6033:3306"
  #   environment:
  #     MARIADB_USER: root
  #     MARIADB_ROOT_PASSWORD: 12345678
  #     MARIADB_DATABASE: inspire

  redis:
    image: ohdaniel/inspire-redis
    container_name: monolithic-redis-container
    ports: 
      - "6379:6379"
    restart: always
